version: "3.4"

services:
  convert:
    build: .
    platform: linux/amd64
    command: npm start
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - DEBUG=f-tempo-conversion:*
    ports:
      - "3000:3000"
